# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
  branches:
    include:
      - main

steps:
- script: |
    git clone https://$(GithubUserName):$(GithubToken)@github.com/Tumm-by/GMI2J3_Projekt.git source
    cd source

    git remote add azure https://$(AzureUserName):$(AzureToken)@dev.azure.com/h11danhc0910/GMI2J3_G7_Project/_git/GMI2J3_Projekt.git
    git push azure --mirror